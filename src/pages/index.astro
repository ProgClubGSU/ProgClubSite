---
import { getEvents, renderPortableText } from "@/lib/sanity.js";
import Hero from "@/components/Hero.astro";
import Breadcrumb from "@/components/Breadcrumb.astro";
import Section from "@/components/Section.astro";
import BlogPostCard from "@/components/BlogPostCard.astro";
import Layout from "@/layouts/Layout.astro";
import { KNOWN_TECH, ABOUT_ME, SITE_TITLE } from "@/consts";
import { separateEventsByDisplayType, sortEventsByPriority, type Event } from "@/utils/eventUtils";

const rawEvents = await getEvents();

// aHelper function to extract plain text from description
function getPlainTextExcerpt(description) {
  if (typeof description === 'string') return description;
  if (!description || !Array.isArray(description)) return '';
  
  // Extract text from block content
  return description
    .filter(block => block._type === 'block')
    .map(block => 
      block.children
        ?.filter(child => child._type === 'span')
        ?.map(span => span.text)
        ?.join('') || ''
    )
    .join(' ')
    .substring(0, 150) + '...';
}

// Helper function to determine event status
function getEventStatus(date) {
  if (!date) return 'upcoming';
  
  const now = new Date();
  const eventDate = new Date(date);
  
  // Set event date to start of day for comparison
  eventDate.setHours(0, 0, 0, 0);
  const today = new Date(now);
  today.setHours(0, 0, 0, 0);
  
  if (eventDate < today) return 'past';
  if (eventDate.getTime() === today.getTime()) return 'active';
  return 'upcoming';
}

// Convert raw events to Event type and sort by priority
const events: Event[] = rawEvents.map(event => ({
  ...event,
  slug: { current: event.slug.current }
}));

const sortedEvents = sortEventsByPriority(events);

// Get only featured and upcoming events (no past events)
const { fullCardEvents, liteCardEvents } = separateEventsByDisplayType(sortedEvents);

// Combine and limit to 3 events for home page (more minimal)
const homePageEvents = [...fullCardEvents, ...liteCardEvents].slice(0, 3);
---

<Layout title={SITE_TITLE}>
  <Hero />
  
  <!-- Our Mission Section -->
  <section class="py-16 px-8 bg-black relative overflow-hidden">
    <!-- Continuing grid background with fade-in effect -->
    <div class="absolute inset-0">
      <div class="w-full h-full opacity-3" style="background-image: linear-gradient(rgba(255,255,255,0.1) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.1) 1px, transparent 1px); background-size: 50px 50px;"></div>
      <!-- Fade-in gradient at top -->
      <div class="absolute top-0 left-0 right-0 h-32 bg-gradient-to-b from-black via-black/80 to-transparent"></div>
    </div>
    
    <div class="max-w-6xl mx-auto relative z-10">
      <!-- Brutalist Section Header -->
      <div class="mb-16 flex items-end justify-between">
        <div>
          <h2 class="text-4xl md:text-6xl font-bold text-white mb-2 tracking-tight" style="font-family: 'Helvetica Neue', sans-serif;">
            our mission
          </h2>
          <div class="text-xs text-white/40 uppercase tracking-widest font-mono">
            // building the future of tech
          </div>
        </div>
        
        <!-- Mission Counter -->
        <div class="text-right">
          <div class="text-2xl font-bold text-white font-mono">
            [01]
          </div>
          <div class="text-xs text-white/40 uppercase tracking-widest">
            core
          </div>
        </div>
      </div>
      
      <!-- Mission Content - Minimal & Profound -->
      <div class="max-w-4xl mx-auto text-center space-y-12">
        <!-- Core Mission Statement -->
        <div class="space-y-6">
          <h3 class="text-3xl md:text-5xl lg:text-6xl font-light text-white leading-tight tracking-tight" style="font-family: 'Helvetica Neue', sans-serif;">
            we are creatives<br/>
            <span class="font-bold">we create the future</span>
          </h3>
          
          <p class="text-lg md:text-xl text-white/60 max-w-2xl mx-auto leading-relaxed font-light" style="font-family: 'Helvetica Neue', sans-serif;">
            ATL's most creative students with the most amazing ideas
          </p>
        </div>
        
        <!-- Philosophy Grid -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 md:gap-6 mt-16">
          <div class="space-y-3">
            <div class="w-12 h-12 mx-auto md:mx-0 bg-white/5 rounded-full flex items-center justify-center">
              <div class="w-3 h-3 bg-white rounded-full"></div>
            </div>
            <div class="text-lg font-medium text-white" style="font-family: 'Helvetica Neue', sans-serif;">think different</div>
            <div class="text-sm text-white/50 leading-relaxed" style="font-family: 'Helvetica Neue', sans-serif;">
              challenge conventions.<br/>question everything.<br/>innovate beyond limits.
            </div>
          </div>
          
          <div class="space-y-3">
            <div class="w-12 h-12 mx-auto md:mx-0 bg-white/5 rounded-full flex items-center justify-center">
              <div class="w-3 h-3 bg-white rounded-full"></div>
            </div>
            <div class="text-lg font-medium text-white" style="font-family: 'Helvetica Neue', sans-serif;">build fearlessly</div>
            <div class="text-sm text-white/50 leading-relaxed" style="font-family: 'Helvetica Neue', sans-serif;">
              fail fast. learn faster.<br/>iterate relentlessly.<br/>ship with confidence.
            </div>
          </div>
          
          <div class="space-y-3">
            <div class="w-12 h-12 mx-auto md:mx-0 bg-white/5 rounded-full flex items-center justify-center">
              <div class="w-3 h-3 bg-white rounded-full"></div>
            </div>
            <div class="text-lg font-medium text-white" style="font-family: 'Helvetica Neue', sans-serif;">impact everything</div>
            <div class="text-sm text-white/50 leading-relaxed" style="font-family: 'Helvetica Neue', sans-serif;">
              code with purpose.<br/>build with meaning.<br/>change the world.
            </div>
          </div>
        </div>
        
        <!-- Minimal Stats -->
        <div class="flex justify-center items-center gap-16 md:gap-24 mt-16 pt-8 border-t border-white/10">
          <div class="text-center">
            <div class="text-2xl md:text-3xl font-light text-white mb-1" style="font-family: 'Helvetica Neue', sans-serif;">500+</div>
            <div class="text-xs text-white/40 uppercase tracking-widest" style="font-family: 'Helvetica Neue', sans-serif;">builders</div>
          </div>
          <div class="text-center">
            <div class="text-2xl md:text-3xl font-light text-white mb-1" style="font-family: 'Helvetica Neue', sans-serif;">∞</div>
            <div class="text-xs text-white/40 uppercase tracking-widest" style="font-family: 'Helvetica Neue', sans-serif;">possibilities</div>
          </div>
          <div class="text-center">
            <div class="text-2xl md:text-3xl font-light text-white mb-1" style="font-family: 'Helvetica Neue', sans-serif;">+100%</div>
            <div class="text-xs text-white/40 uppercase tracking-widest" style="font-family: 'Helvetica Neue', sans-serif;">ideas</div>
          </div>
        </div>
      </div>
    </div>
  </section>
  
  <!-- Events Section -->
  <section class="py-16 px-8 bg-black relative overflow-hidden">
    <!-- Continuing grid background -->
    <div class="absolute inset-0">
      <div class="w-full h-full opacity-3" style="background-image: linear-gradient(rgba(255,255,255,0.1) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.1) 1px, transparent 1px); background-size: 50px 50px;"></div>
    </div>
    
    <div class="max-w-6xl mx-auto relative z-10">
      <!-- Brutalist Section Header -->
      <div class="mb-16 flex items-end justify-between">
        <div>
          <h2 class="text-4xl md:text-6xl font-bold text-white mb-2 tracking-tight" style="font-family: 'Helvetica Neue', sans-serif;">
            events
          </h2>
          <div class="text-xs text-white/40 uppercase tracking-widest font-mono">
            // upcoming workshops & meetups
          </div>
        </div>
        
        <!-- Event Counter -->
        <div class="text-right">
          <div class="text-2xl font-bold text-white font-mono">
            [{homePageEvents.length.toString().padStart(2, '0')}]
          </div>
          <div class="text-xs text-white/40 uppercase tracking-widest">
            active
          </div>
        </div>
      </div>
      
      <!-- Events Content -->
      {homePageEvents.length > 0 ? (
        <>
          <!-- Events List - Brutalist Style -->
          <div class="space-y-1 mb-12">
            {
              homePageEvents.map((event, index) => {
                const eventDate = event.date?.isTBD ? null : (event.date?.value ? new Date(event.date.value) : null);
                const formattedDate = eventDate ? eventDate.toLocaleDateString('en-US', { 
                  month: '2-digit', 
                  day: '2-digit',
                  year: '2-digit'
                }) : 'TBD';
                
                return (
                  <a 
                    href={`/events/${event.slug.current}`}
                    class="block border border-white/10 hover:border-white/30 bg-white/[0.02] hover:bg-white/[0.05] transition-all duration-200 group"
                  >
                    <div class="px-6 py-4 flex items-center justify-between">
                      <!-- Left: Index & Title -->
                      <div class="flex items-center space-x-6">
                        <div class="text-white/40 font-mono text-sm min-w-[3rem]">
                          {(index + 1).toString().padStart(2, '0')}.
                        </div>
                        <div>
                          <h3 class="text-white group-hover:text-white font-medium text-lg tracking-tight" style="font-family: 'Helvetica Neue', sans-serif;">
                            {event.title}
                          </h3>
                          <div class="text-white/50 text-sm font-mono mt-1">
                            {getPlainTextExcerpt(event.description).substring(0, 60)}...
                          </div>
                        </div>
                      </div>
                      
                      <!-- Right: Date & Status -->
                      <div class="text-right flex items-center space-x-4">
                        <div>
                          <div class="text-white/60 font-mono text-sm">
                            {formattedDate}
                          </div>
                          {event.time?.value && (
                            <div class="text-white/40 font-mono text-xs">
                              {event.time.value}
                            </div>
                          )}
                        </div>
                        <div class="text-white/40 group-hover:text-white/60 transition-colors">
                          →
                        </div>
                      </div>
                    </div>
                  </a>
                );
              })
            }
          </div>
          
          <!-- Terminal-style Button -->
          <div class="border-t border-white/10 pt-8">
            <a 
              href="/events" 
              class="inline-flex items-center text-white/60 hover:text-white font-mono text-sm transition-colors duration-200 group"
              style="font-family: 'Courier New', monospace;"
            >
              <span class="mr-2 text-green-400 group-hover:text-green-300">$</span>
              <span class="group-hover:text-green-100">ls /events --all</span>
              <span class="ml-2 text-white/30 group-hover:text-white/50">▊</span>
            </a>
          </div>
        </>
      ) : (
        <!-- No Events - Terminal Style -->
        <div class="border border-white/10 bg-white/[0.02] p-8">
          <div class="font-mono text-white/60 space-y-2">
            <div class="text-green-400">$ ls events/</div>
            <div class="text-white/40">ls: cannot access 'events/': No such file or directory</div>
            <div class="text-white/30 mt-4">// check back soon for new workshops</div>
          </div>
        </div>
      )}
    </div>
  </section>
  {/* Technologies section - commented out for now, may be used in the future */}
  {/* <Section title="Technologies I Know" full_screen={false}>
    <div class="w-full grid place-items-center">
      <div
        class="w-full gap-4 flex flex-wrap px-3 max-w-xl text-lg justify-center"
      >
        {KNOWN_TECH.map((x) => <Breadcrumb title={x} />)}
      </div>
    </div>
  </Section> */}
  <Section title="About">
    <div class="w-full grid place-items-center">
      <div
        class="max-w-4xl text-xl md:text-lg opacity-90 text-center justify-evenly"
      >
        <p>{ABOUT_ME}</p>
      </div>
    </div>
  </Section>
</Layout>
